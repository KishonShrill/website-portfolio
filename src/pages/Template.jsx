import { useEffect } from "react"

const Template = (props) => {
  useEffect(() => {
    document.title = props.title
    import('')
  }, [])

  return(
    <>
    
    </>
  )
}
export default Template